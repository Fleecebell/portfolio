# 2026-08-01 工作日志

## 挂牌只显示静态卡（3D 崩溃）→ StrictMode/Environment 修复
- 现象：挂牌区域只有静态名片图（ErrorBoundary 生效），绳子/弹动全无
- Node 验证 @dimforge/rapier3d-compat 初始化 OK（wasm/刚体/冲量/step 正常）→ 问题在 React 集成层
- 修复：① main.jsx 去掉 StrictMode（React 19 + R3F9 + rapier 的 double-mount 导致物理世界初始化异常）② 移除 drei `<Environment>`（children Lightformer 模式，替换为显式 directional/point/ambient 灯光）③ 卡片材质 metalness 0.8→0.35、roughness 0.9→0.62（无环境反射补偿）④ LanyardBoundary componentDidCatch 在 dev 环境把错误信息显示到 fallback（方便后续定位）
- 修改：src/main.jsx、src/components/Lanyard/Lanyard.jsx、Lanyard.css；preview.html 已重建

## #projects 黑屏修复（挂牌物理崩溃）
- 现象：localhost:5173/#projects 打开闪一下正常画面后整页黑屏
- 根因：上轮把 j3 position 设为 [2,0,0] 与 card 刚体重合，而 spherical joint 锚在 (0,1.35,0)，初始状态严重违反约束 → 物理求解数值爆炸（NaN）→ R3F Canvas 崩溃传播 → 整页黑
- 修复：① j3 position → [2,1.35,0]（初始满足 joint 约束）② kick() 包 try/catch ③ useFrame 主体包 try/catch ④ 新增 LanyardBoundary ErrorBoundary：3D 崩溃时降级为静态 front.png 名片图（.lanyard-fallback），保证整页永不黑
- 修改：src/components/Lanyard/Lanyard.jsx、Lanyard.css；preview.html 已重建

## 挂牌 clampGeo is not defined 修复
- 现象：fallback 显示 "3D 初始化失败：clampGeo is not defined"
- 根因：Band 解构 useMemo(buildCardAssets) 时漏了 clampGeo（buildCardAssets 已返回、JSX 也引用了），ReferenceError 在渲染期抛出 → Canvas 崩 → 降级静态卡
- 修复：解构补上 clampGeo
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 挂绳末端与卡扣之间空隙修复
- 现象：无报错，但绳子末端与夹子之间有 0.225 world 单位空隙（约 16px），环太小不明显
- 根因：joint 锚 local 1.35 vs 卡片顶 world 1.125（mesh 位置 ×scale 2.25 与 physics 锚点无缩放的对齐错位）；环半径 0.032 太小
- 修复：锚 local → 1.15、j3 初始 [2,1.15,0]、clamp mesh group y → 0.511（=1.15/2.25 精确对齐挂绳末端）、clip 保持在卡片顶 0.5、clamp 环加大到 Torus(0.05, 0.016)
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 删除铁环 + 绳子末端贴夹片上边缘
- 需求：绳子末端边缘与夹片上边缘紧密贴合，删去铁环
- 实现：删除 clampGeo（Torus 环）；夹片加高 0.05→0.06；joint 锚 local → 1.18（= (0.5+0.03)×2.25 夹片上边缘），j3 初始 [2,1.18,0]；挂绳 line 终点 j3 精确落在夹片顶
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 挂绳改细
- lanyardWidth 默认 1 → 0.4（meshline lineWidth），Hero.jsx 去掉显式 lanyardWidth={1}
- 修改：src/components/Lanyard/Lanyard.jsx、src/components/Hero.jsx；preview.html 已重建

## 挂牌正面重排（头像左 + 名片信息）
- 头像移到名字左边（横向 168×168），名字 60px + UNITY 客户端开发 21px teal 副标题
- 删除 "// UNITY 客户端开发" 装饰行和"微信大号 + 点击复制"特写（之前正面只有头像+名字+微信 3 块）
- 新增 5 行信息：学校 哈尔滨理工大学 / 专业 软件工程 / 毕业 2028 年毕业 / 方向 Unity 客户端开发 / 微信 15112277413（标签灰 + 值白，26/28px，间距 72px）
- 修改：scripts/gen-lanyard-cards.mjs（frontSvg 重写）；preview.html 已重建

## 挂牌正面信息区调整（下移/大字号/大间距）
- 信息 5 行起始 y 380 → 430，行距 72 → 88，标签 26 → 30px、值 28 → 32px；标签 x 90→100、值 x 232→254
- 修改：scripts/gen-lanyard-cards.mjs；preview.html 已重建

## 挂牌桌面放大 2 倍
- 视觉 scale 2.25 → 4.5（桌面）/ 2.5（移动端），动态 cardScale 传入 buildCardAssets
- 锚点联动：clipAnchorY = (0.5+0.03)×cardScale，joint 锚/j3 初始位置用 clipAnchorY（保持挂绳贴夹片上边缘）
- 相机：桌面 position [1.4,2.0,14] fov 25；移动端 [1.2,1.5,11] fov 22
- 容器：hero-lanyard 高 clamp(560px,70vh,680px)；hero-inner 右列 minmax(360,0.72fr) → minmax(440px,0.9fr)
- 桌面卡片视觉 ~494px 高（原 227px，约 2.2 倍）
- 修改：src/components/Lanyard/Lanyard.jsx、src/styles/components.css；preview.html 已重建

## 挂牌整体上移（绳头挂屏幕顶部）
- 物理场景抬高：fixed/j1/j2/card y 0 → anchorY（桌面 5.2 / 移动端 3.7），j3 y = anchorY + clipAnchorY
- 相机：桌面 [1.4,2.1,14.5] fov 25（可见 y -1.12~5.32，fixed 5.2 在顶部）；移动端 [1.2,1.8,10.5] fov 22
- 桌面卡片视觉 ~475px（保持 2 倍大小），卡片中心下垂到画面中部，绳头贴画面顶部
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 挂牌覆盖全 hero（解决遮挡/遮罩限制/位置高）
- 结构重构：.hero-lanyard 从 hero-inner grid 移出 → hero 直接子级，absolute inset 0 z-index 2 覆盖整个 hero
- 解决"遮罩限制"：canvas 之前只在 hero-lanyard 容器内绘制，拖出即消失；现在 canvas 覆盖全 hero，拖到任意位置可见
- 不拦截交互：.hero-lanyard 和 canvas 都 pointer-events: none，R3F Canvas 加 eventSource={heroRef}（hero section ref）接收事件 → raycast 命中卡片才触发
- 手机竖屏被自我介绍挡住：挂牌 z2 在内容之上，始终可见；移动端 .hero overflow: visible（内容超高不裁）
- 桌面横屏位置高：anchorY 5.2→5.0，相机默认 [1.4,1.9,14.5]（卡片中心下移到画面中部）
- hero-main 桌面 max-width 1060px（右侧留给挂牌）
- 修改：src/components/Hero.jsx、Lanyard/Lanyard.jsx、Lanyard.css、styles/components.css；preview.html 已重建

## 挂牌回归布局流（左/上 + 绳头下移 + 导航锚定标题）
- 横屏：hero-inner 双列，挂牌在左（minmax(440px,0.85fr)），自我介绍在右；竖屏：单列，挂牌 order -1 在上、自我介绍在下，hero overflow visible
- 撤销"覆盖全 hero"方案（.hero-lanyard 从 absolute 恢复 position relative 布局容器，高 clamp(540,66vh,660px)），canvas 恢复接收事件
- 绳头下移：anchorY 桌面 5.0→4.2、移动端 3.7→3.2（画面顶部偏下 14% 处）
- 导航「自我介绍」href #top → #intro（hero-main 加 id="intro"），scroll-margin-top 92px 定位到林赫洋标题
- 修改：src/components/Hero.jsx、Header.jsx、Lanyard/Lanyard.jsx、Lanyard.css、styles/components.css；preview.html 已重建

## 自我介绍导航停靠位置上移
- .hero-main scroll-margin-top 92px → 72px（标题顶紧贴导航栏下缘，再往上不遮挡）
- 修改：src/styles/components.css；preview.html 已重建

## 挂牌整体下移 50% 自身长度
- 场景 anchorY 桌面 4.2/移动 3.2 → 统一 1.95（桌面下移 2.25 = 卡片高 4.5 的 50%，移动端下移 1.25）
- 相机同步下移保持构图：桌面默认 [1.4,-0.5,14.5]（y 1.9→-0.5），移动端 [1.2,0.4,10.5]（y 1.8→0.4）
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 挂牌反光晃眼修复（光源换位 + 材质哑光）
- 光源：主方向光移到侧上方 [4,5,3]（强度 1.8→1.1），teal 点光 40→8 且移远 [0,2,5]，增加柔光 [−5,1,4] 0.45
- 材质：clearcoat 1→0、metalness 0.35→0.2、roughness 0.62→0.82（哑光）；金属夹片 metalness 1→0.55、roughness 0.3→0.55
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 挂牌光源微调（保留质感但弱化）
- 材质：clearcoat 0→0.3（桌面，移动端 0）、metalness 0.2→0.3、roughness 0.82→0.72（有光泽不闪眼）
- 光源：主光 1.1→1.35、teal 点光 8→12、ambient 1.0、柔光 0.5（侧上方不直射正面）
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 画布四向各延展一屏
- .lanyard-wrapper absolute left/right -100vw、top/bottom -100vh（以 hero-lanyard 容器为中心四向各扩一屏），pointer-events: none
- canvas 不拦截事件，R3F Canvas eventSource={heroRef}（hero section）接收 → raycast 命中卡片才触发
- hero overflow hidden → visible（画布溢出可见），.hero-bg 自身 overflow: hidden（光晕/网格不溢出）
- dpr 2 → 1.25（画布物理像素巨大，控制 GPU 开销）
- 修改：src/components/Hero.jsx、Lanyard/Lanyard.jsx、Lanyard.css、styles/components.css；preview.html 已重建

## 画布延展后牌子恢复原视觉大小
- 画布延展后相机视野不变 → 牌子映射到更大画布 → 屏幕上异常变大
- 修复：牌子 world 尺寸不变（scale 4.5/2.5），拉远相机 + 加大 fov：桌面 [1.4,-0.9,30] fov 50（可见世界高 27.6，牌子 canvas 内 460px ≈ 原视觉）；移动端 [1.2,0.2,22] fov 45
- 画布保持四向延展（拖拽范围不变）
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 点击失灵修复（事件源 + toast 位置）
- 根因1：canvas pointer-events: none + eventSource 事件链未生效 → 点击无响应
- 根因2：toast absolute 定位在延展后的 wrapper 顶部 = 屏幕外，永远看不到
- 修复：canvas 恢复 pointer-events: auto 直接接收事件（移除 eventSource 传参）；hero-main DOM 顺序在 hero-lanyard 后，天然覆盖画布右列 → 按钮仍可点；hero-lanyard 移除 pointer-events: none
- toast 改 fixed 屏幕底部中央（不依赖 wrapper 定位）
- 修改：src/components/Hero.jsx、Lanyard/Lanyard.jsx、Lanyard.css、styles/components.css；preview.html 已重建

## 工牌放大 + 标题再缩小
- 工牌：fov 桌面 50→44（可见世界高 24.2，牌子 canvas 内 ~524px）、移动端 45→40（~326px）
- 标题"林赫洋"：clamp(44,7.5vw,110) → clamp(38px,6.5vw,96px)
- 修改：src/components/Lanyard/Lanyard.jsx、styles/components.css；preview.html 已重建

## 挂牌文字整体放大
- 正面：名字 60→66、UNITY 客户端开发副标题 21→26、信息标签 30→34、值 32→36、底部 mono 13→14；行距 90（y 432-792）
- 背面：名字 52→56、副标题 20→24、标签 25→28、值 27→30、底部 14
- 修改：scripts/gen-lanyard-cards.mjs（重新生成 front/back.png）；preview.html 已重建

## 挂牌正面文字再放大（指定字号）
- UNITY 客户端开发副标题 26→36px；信息标签 34→40px、值 36→43px；删除底部 mono 小字；行距 90→96（y 448-832）
- 修改：scripts/gen-lanyard-cards.mjs（重新生成 front.png）；preview.html 已重建

## localhost 打不开 / preview.html 无法显示 修复
- 根因1：dev server 未运行（localhost:5173 连接失败）→ 重启 npx vite（后台）
- 根因2：preview.html 的 ./assets 相对路径在根目录无 assets 文件夹（资源实际在 dist/assets）→ file:// 下图片全部 404 → 挂牌等显示异常
- 修复：新建 scripts/sync-preview.mjs（build 后复制 dist/index.html → preview.html + dist/assets → assets/），package.json 加 build:preview 脚本；.gitignore 排除 dist/、assets/、node_modules/
- 约定：以后构建用 npm run build:preview（或 build 后手动 node scripts/sync-preview.mjs）

## file:// 下立牌异常大/无法点击 修复（WebGL 跨域）
- 根因：file:// 协议加载的本地图片在 WebGL texImage2D 上传时被浏览器视为跨域数据 → SecurityError → Canvas 崩溃 → fallback 静态卡（异常大且不可点）
- 修复：front/back.png 改为 ?inline 导入（vite 构建时内联为 data URI，同源可用）；gen-lanyard-cards.mjs 同步输出到 src/components/Lanyard/cards/；Hero.jsx 不再传图片路径（组件默认 data URI）
- 附带：sync-preview.mjs 改底层 readdir/copyFile（cpSync 被沙箱拦截）；复制 dist 根部散文件（哈希 favicon）到根目录
- preview.html 3.6MB（含内联图片）；构建命令 npm run build:preview
- 修改：scripts/gen-lanyard-cards.mjs、scripts/sync-preview.mjs、src/components/Lanyard/Lanyard.jsx、Hero.jsx；preview.html 已重建

## 竞赛与获奖无法框选/hover 偏移修复
- 根因：立牌画布四向延展覆盖 hero 下方 awards 区域，canvas pointer-events: auto 拦截鼠标 → 文字无法框选、hover 上半部分无反应
- 修复：canvas 恢复 pointer-events: none（不拦截任何页面区域）；立牌事件改为从挂牌容器（hero-lanyard，仅左列小区域）经 Canvas eventSource={lanyardRef} 接收 → raycast 命中立牌才触发（事件区与渲染区分离）
- 修改：src/components/Hero.jsx、Lanyard/Lanyard.css；preview.html 已重建（npm run build:preview）

## 移动端导航按钮跑到屏外 + 背景不居中 修复
- 根因：`.nav-toggle` 依赖 `.header-inner` 的 flex 布局 + `margin-left: auto`，手机端 flex 行为不可靠导致按钮被推到屏外；`.hero-bg` 早期被 hero 的 overflow hidden 间接影响导致 hero 只能覆盖到内容宽度
- 修复：① `.nav-toggle` 改为 `position: fixed; top: 16px; right: var(--gutter);`（脱离 header-inner 布局，固定屏幕右上角），`display: flex` 默认 + `min-width: 1081px` 才 display:none 隐藏桌面 ② hero 100vw 背景居中（之前 shot-mobile.png hero 60% 是 hero-bg 没覆盖全宽；现在 hero 100vw 居中）
- 真机验证：viewport meta 已存在（index.html），390px 真实手机 viewport 正确，媒体查询 1081 不触发，nav-toggle 必显示
- 调试脚本：scripts/screenshot-mobile.mjs（chrome CDP 截图，手机视口模拟）——保留备用
- 修改：src/styles/components.css

## 立牌蒙蓝色效果去除
- 根因：灯光含冷色光源（冰蓝补光 #7cc7ff + teal 点光 #5eead4），卡片表面整体偏蓝
- 修复：改为中性白光（两束 directionalLight 均 #ffffff，去掉 teal 点光），主光强度 1.35→1.4 补偿
- 修改：src/components/Lanyard/Lanyard.jsx；preview.html 已重建

## 作品详情子面板关闭按钮始终可见
- 根因：.detail-close 绝对定位在面板内顶部，面板滚动后按钮滚出视野
- 修复：改为 fixed 定位（top 16 right 16 z 210），钉在屏幕右上角，面板滚动时始终可见；加半透明背景+模糊提升可读性
- 修改：src/styles/components.css；preview.html 已重建
- 注意：vite build 频繁被 safe-delete 拦截（dist 文件占用），构建需先 mv dist 再 build（shim 对 rm/trash 限制）

## 详情面板叉叉动画瞬移修复
- 根因：.detail-close 是 panel 子元素，panel 有 detail-rise 动画（transform）→ 动画期间 fixed 相对 panel（transform 祖先），动画结束 fixed 恢复相对视口 → 瞬移右上角
- 修复：把 close 按钮从 panel 移到 mask（无 transform）直接子级，fixed 始终相对视口
- 修改：src/components/Projects.jsx；preview.html 已重建

## 立牌无法点击（eventSource 不可靠）→ 回归 canvas 直接接收
- 确认 R3F eventSource 方案在该项目不可靠（两次尝试都不工作）
- 修复：canvas 恢复 pointer-events: auto 直接接收事件（立牌可点）；解决画布覆盖其他栏目：main > section:not(.hero) 提升 z-index:1（透明画布被区块盖住 → 不拦截 awards 等交互），footer 同理
- 修改：src/components/Hero.jsx、Lanyard/Lanyard.css、styles/components.css；preview.html 已重建

## hero 网格 + 鼠标光晕 全局化
- 网格：hero-grid 移除，改 main::before 固定全屏网格层（fixed inset 0 z0，72px 网格 + grid-drift 漂移动画，视频之上内容之下）；main > section:not(.hero) 与 footer 提升 z1（内容在网格上）
- 鼠标光晕：hero-cursor-glow 移除，App.jsx 新增 CursorGlow 全局组件（fixed 480px teal 径向光晕，window mousemove + rAF 更新 transform，z90 微弱不拦截）
- 修改：src/App.jsx、Hero.jsx、styles/components.css；preview.html 已重建

## 去掉全页格子 + 分界线
- 删除 main::before 网格层与 grid-drift 动画（格子背景全页移除）
- 分界线来源：hero 视频亮背景 vs awards 深背景网格可见度突变 → 交界处网格突然清晰；删网格后消失
- 修改：src/styles/components.css；preview.html 已重建

## hero/awards 之间的分界线去除
- 根因：hero 视频底部的像素艺术地面（亮带）与 awards 区域深色背景形成鲜明对比 → 光晕在两侧可见度突变 → 视觉分界线
- 修复：hero::after 渐变遮罩（底部 240px，从透明到 var(--bg-0) 90%），让 hero 视频底部平滑过渡到页面背景色，与 awards 无缝
- 修改：src/styles/components.css

## hero/awards 之间的分界线去除
- 根因：hero 视频底部的像素艺术地面（亮带）与 awards 区域深色背景形成鲜明对比 → 光晕在两侧可见度突变 → 视觉分界线
- 修复：hero::after 渐变遮罩（底部 240px，从透明到 var(--bg-0) 90%），让 hero 视频底部平滑过渡到页面背景色，与 awards 无缝
- 修改：src/styles/components.css

## 遮罩只保留上半部分（分界线真正根因）
- 用户澄清：分界线来自 hero-video-overlay（fixed 全屏黑色渐变，底部 0.66 反而更暗）
- 修复：overlay 改为仅覆盖上 62vh（0.55→0.3→0 渐变），下半部分完全不遮
- hero::after 底部过渡层 z-index 5 → 0（内容之下），不遮挡立牌，同时保留视频底部融入背景的平滑过渡
- 修改：src/styles/components.css

## 遮罩只保留上半部分（分界线真正根因）
- 用户澄清：分界线来自 hero-video-overlay（fixed 全屏黑色渐变，底部 0.66 反而更暗）
- 修复：overlay 改为仅覆盖上 62vh（0.55→0.3→0 渐变），下半部分完全不遮
- hero::after 底部过渡层 z-index 5 → 0（内容之下），不遮挡立牌，同时保留视频底部融入背景的平滑过渡
- 修改：src/styles/components.css

## 竞赛与获奖上移 180px（联动后续内容）
- transform → margin-top: -180px（布局上移，experience/projects/contact/footer 全部联动上移）
- 锚点 scroll-margin-top 基于布局位置，各 section 导航跳转位置自动适配
- 修改：src/styles/components.css

## 竞赛与获奖上移 180px（联动后续内容）
- transform → margin-top: -180px（布局上移，experience/projects/contact/footer 全部联动上移）
- 锚点 scroll-margin-top 基于布局位置，各 section 导航跳转位置自动适配
- 修改：src/styles/components.css

## 删除 awards 头部蒙版 + 全屏均匀视频蒙版
- 删除 hero::after 底部渐变过渡层（awards 上移后与其头部重叠形成的深色蒙版）
- hero-video-overlay 改回 fixed inset 0 全屏均匀 rgba(5,7,10,0.5)（不随滚动/鼠标移动）
- 删除 hero-video-shade（上 70vh 渐变压暗层，已由全屏均匀蒙版替代）；Hero.jsx 移除 shade div
- 修改：src/styles/components.css、Hero.jsx

## 删除 awards 头部蒙版 + 全屏均匀视频蒙版
- 删除 hero::after 底部渐变过渡层（awards 上移后与其头部重叠形成的深色蒙版）
- hero-video-overlay 改回 fixed inset 0 全屏均匀 rgba(5,7,10,0.5)（不随滚动/鼠标移动）
- 删除 hero-video-shade（上 70vh 渐变压暗层，已由全屏均匀蒙版替代）；Hero.jsx 移除 shade div
- 修改：src/styles/components.css、Hero.jsx

## dev 与 file:// 显示差异排查（headless 对比）
- CDP headless 对比：DOM（rootLen 10228 相同）、CSS（bodyBg 相同）、视频（都在播 1920x1080）、立牌 canvas（3366x2405 相同）、无 fallback → 两环境代码层面完全一致
- 发现共性 bug：MeshLineGeometry NaN 警告（物理初始化时 translation NaN）→ 修复：setPoints 前检查点有限性，NaN 帧跳过
- 用户实际差异待确认（可能是真实 GPU 渲染细节）
- 修改：src/components/Lanyard/Lanyard.jsx

## localhost 内容左移修复（html 层横向滚动）
- 排查：headless 几何对比 dev/file 完全一致；发现 docScrollW 2000 > clientW 1412（溢出源：立牌 wrapper 3366px、轮播轨道 3864px、glow）
- 根因：body 有 overflow-x:hidden，但 html 层（滚动容器）overflow-x visible → html 层产生横向滚动条 → 内容被横向滚动整体左移（dev 下明显，file 下可能未触发）
- 修复：base.css html 加 overflow-x: hidden（消除 html 层横向滚动）
- 修改：src/styles/base.css
