# 2026-07-31 工作日志

## 挂牌衔接修复（工牌结构：孔 + 卡扣）
- 消除悬空：group position [0,-1.2,-0.05] → [0,0,0]（mesh 与物理对齐）；j3 position [1.5,0,0] → [2,0,0]（与 card RB 同位置）；spherical joint 锚 [0,1.5,0] → [0,1.35,0]（卡扣顶端，与挂绳 line 终点 j3 重合）→ 挂绳视觉直接连到卡扣
- 删除 clip 灰色挡板，改成工牌卡扣（鳄鱼夹简化）：BoxGeometry(0.22, 0.05, 0.08) 夹片 + TorusGeometry(0.032, 0.012) 顶端连接环
- front.png 卡片顶部加两个穿孔（深色实心圆，模拟工牌孔）
- Toast 位置：fixed bottom 13% → absolute 在 lanyard-wrapper 顶部 top 14px，避开卡片中央文字
- 修改：src/components/Lanyard/{Lanyard.jsx,Lanyard.css}、scripts/gen-lanyard-cards.mjs；preview.html 已重建

## 挂牌视觉重构（消拉伸 / 删丑环 / 移夹子 / 放大字）
- 拉伸：UV 矩形比例与卡片几何不匹配 + cover 裁切 → atlas 改为 1422×1000（左右半区 711×1000 = 卡片 0.711:1 = 图片比例），FRONT/BACK_UV_RECT 改为 {w:0.5, h:1}；atlas texture 关闭 mipmap（NPOT）；几何正面/背面 UV 改为 0-0.5/0.5-1 × 0-1
- 丑挂环：删除 clamp Torus 环
- 灰色矩形挡字：clip 原本在 y=-0.14 卡片正面中部，盖住名字 → 移到顶部边缘 y=0.5，尺寸缩小到 0.42×0.1×0.05
- 字太小：正面精简为头像+名字+角色+微信大号（设计稿 62px 等宽字体），背面包含完整 5 行信息（学校/专业/毕业/方向/微信）；相机默认 z 12 fov 25，移动端 z 9 fov 22
- front.png / back.png 重新生成（711×1000 1:1.41 竖版，与卡片几何同比例）
- 修改：src/components/Lanyard/Lanyard.jsx（UV/几何/atlas/clip 删除/相机）、scripts/gen-lanyard-cards.mjs；preview.html 已重建

## 挂牌尺寸缩小 + 出界裁切修复
- 用户反馈：挂牌太大 + 弹动到旁边被"遮罩挡住"
- 根因：WebGL canvas 只绘制在自身矩形内，弹动甩出 .hero-lanyard 容器边界即消失（视觉似遮罩）
- 修复：相机拉远广角（桌面 position [1.4,1.7,14] fov 28，卡片占视口 44%→32%；移动端 [1.2,1.5,10] fov 22）；相机 x 偏移对准摆动中心（避免卡片出左右画）；kick 力度降（定时 0.45→0.3、点击 2.4→1.4，y 分量收敛）
- Hero.jsx 去掉显式 position/fov 传参（用组件默认）
- 修改：src/components/Lanyard/Lanyard.jsx、src/components/Hero.jsx；preview.html 已重建

## Lanyard 挂牌交互增强（弹动/点击/拖拽/复制）
- 每 5 秒定时随机轻弹（kick power 0.45，桌面端；拖拽中跳过）；点击（位移<8px 且时长<700ms）大力度弹动（power 2.4）+ 复制电话；拖拽结束也复制电话
- 复制：navigator.clipboard（secure context）+ legacyCopy（textarea+execCommand，file:///微信兜底）；Toast "已将电话/微信复制到剪切板"（fixed 底部 13%，2.2s 自动消失）
- 移动端：不做拖拽（onPointerDown isMobile return），保留点击交互；canvas touch-action: pan-y 保证垂直滚动阅读顺畅；相机拉近（[0,1.4,8.6] fov 20）挂牌更大文字更清晰；hero-lanyard 高度 460px
- 剪贴板号码常量 phone prop（默认 15112277413）
- 单文件构建 dist/index.html 3.48MB（three+drei+rapier 内联，gzip 1.18MB）——明显变大，后续注意
- 修改：src/components/Lanyard/Lanyard.jsx、Lanyard.css、src/styles/components.css；preview.html 已重建
- 用户偏好：所有任务自行执行不弹窗（已写入 ~/.workbuddy/MEMORY.md）

## Lanyard 3D 挂牌集成（自我简介装进挂牌）
- 网络受限无法下载 React Bits 官方 card.glb / lanyard.png → 改为运行时构建模型：组件内用 three.js 手写 BufferGeometry（卡片 UV 左半/右半对齐 FRONT_UV/BACK_UV_RECT）+ 金属 Box/Torus + CanvasTexture atlas/挂绳纹理，完全不依赖外部 .glb/.png
- 升级 React 18→19（@react-three/fiber@9 要求 react>=19<19.3 → 装 react@19.2.0）
- 新增依赖：react@19 react-dom@19 three@0.185 meshline @react-three/fiber@9 @react-three/drei@10 @react-three/rapier@2 sharp
- 身份卡内容→挂牌正面 PNG（sharp + SVG）：头像、林赫洋、// UNITY 客户端开发、学校/专业/毕业/方向/微信 5 行 + 底部 mono 字；背面：中央"林"字 + 名字 + 微信 + GitHub 提示；500×1000 竖版（1:2 适配卡片模型）
- Hero.jsx：原静态 .hero-card 替换为 .hero-lanyard 容器 + Lanyard 组件（frontImage/backImage 传图片 URL）；CSS .hero-lanyard 高 clamp(420,52vh,540px)
- Lanyard.jsx wrapper 高度改为 100%（父容器控制，不再 100vh）
- 组件 CSS/JS/vite 全部本地化，无需网络
- 修改/新建：src/components/Lanyard/{Lanyard.jsx,Lanyard.css}、scripts/gen-lanyard-cards.mjs、src/components/Hero.jsx、src/styles/components.css；dev server 已重启到 5173

## 身份卡电话/微信标签调整
- 标签从"电话/微信"改为"微信"（用户去掉"电话/"），值 15112277413 不变
- 修改：`src/components/Hero.jsx`；preview.html 已重建

## 身份卡新增电话/微信
- Hero 右侧身份卡（hero-card）方向行后新增一行：`电话/微信 15112277413`（k mono 标签 + v 值）
- 修改：`src/components/Hero.jsx`；preview.html 已重建

## Projects 区块压缩（第三版：离标题更近 + 上下间距更小）
- `#projects .section-head` margin-bottom → clamp(12,1vw,18)；`.pm-rows` gap → clamp(14,1.5vw,24)
- `#projects` padding-top → clamp(40,5vw,72)、padding-bottom → clamp(48,6vw,80)；scroll-margin-top 联动 calc(100px - clamp(...))
- 桌面核算：标题 100px，第二排底 ~695px ≤ 950px 视口
- 修改：`src/styles/components.css`；preview.html 已重建

## Projects 区块再压缩（第二版）
- `#projects .section-head` margin-bottom → clamp(16,1.5vw,24)；`.pm-rows` gap → clamp(18,2vw,32)
- `#projects` padding-top → clamp(56,7vw,100)（原 --section-gap 160）、padding-bottom → clamp(64,8vw,112)；scroll-margin-top 联动 `calc(100px - clamp(...))` 保持锚定后标题在视口 ~100px
- 桌面核算：标题 100px，第二排底 ~709px ≤ 950px 视口
- 修改：`src/styles/components.css`；preview.html 已重建

## Projects 区块压缩：标题+两排一个视口装下
- 两排上移靠紧标题：`#projects .section-head` margin-bottom 从 clamp(40,5vw,64) → clamp(20px,2vw,32px)；`.pm-rows` gap 从 clamp(32,5vw,72) → clamp(24px,3vw,44px)
- 桌面锚定后：标题顶约 100px，第二排底约 719px ≤ 950px 视口 ✓；移动端 3vw gap 自动收窄
- 修改：`src/styles/components.css`；preview.html 已重建

## 两排间距加大 + 暂停条件改为"仅悬停封面时"
- 间距：`.pm-rows` gap 14px → `clamp(32px, 5vw, 72px)`，两排上下分开
- 暂停：从容器级 mouseenter/mouseleave 改为**卡片级** onMouseEnter（setHovered(i)+setPaused(true)）/ onMouseLeave（恢复）——鼠标在封面之间的间隙、标题区等处都保持滚动，只有悬停在封面上才两排同时暂停（交错恒定）；触摸逻辑不变
- 修改：`src/components/Projects.jsx`、`src/styles/components.css`；preview.html 已重建

## 导航高亮修复 + 作品类型缩写
- 导航高亮：原 IntersectionObserver + 5vh 窄观察带（rootMargin -40%/-55%）滚动经过超长 Projects 区块时漏触发（只经过 02/04 跳过 03）→ 改为**视口中心线检测**：probe = scrollY + 45% 视口高，穿过哪个 section 高亮哪个；getBoundingClientRect 实时计算（封面加载改变高度也准确）；scroll+resize 监听
- 作品小字：type 字段形如 "2D Roguelike / GMTK GameJam 2025"，需求只保留 "/" 前类型 → Projects.jsx 新增 `shortType()`（split(" / ")[0]），buildItems 中统一处理，轮播 hover info + 详情面板 detail-type 同时生效；不动 data.js
- 修改：`src/components/Header.jsx`、`src/components/Projects.jsx`；preview.html 已重建

## 第二排额外左错开半个封面（3.5 卡位）
- 需求：第二排在 offset=3 基础上再往左错半卡位（半封面+间距的一半）
- 实现：`@keyframes pm-marquee-offset`（from: -50% - 0.5卡位，to: -0.5卡位），位移差仍=1 份循环宽→无缝；`.pm-row:nth-child(2) .project-marquee-track { animation-name: pm-marquee-offset }`
- 关键：calc 混用 % 和 px，CSS 变量适配响应式；暂停/时长规则继承不受影响
- 修改：`src/styles/components.css`；preview.html 已重建

## 两排悬浮统一暂停（保证交错恒定）
- 需求：悬浮任意一排时两排都停，确保上下交错关系不变、同作品恒距最远
- 实现：`pm-rows` 容器级 onMouseEnter/onMouseLeave 控制 `paused` 状态 → CSS `.pm-rows.paused .project-marquee-track { animation-play-state: paused }`；卡片 hovered 只用于 info 显示；触摸 onTouchStart/onTouchEnd 同步暂停
- 原单排 `.project-marquee.paused` 规则已替换为容器级规则
- 修改：`src/components/Projects.jsx`、`src/styles/components.css`；preview.html 已重建

## 两排滚动：统一向右 + 修复周期跳变 bug
- 需求：两排都向右滚动，同作品保持距离最远（半周期错位 offset=floor(n/2) 已满足，同步同向恒定）
- **Bug 根因**：动画位移 -33.3333% 与轨道不匹配——loop 实际是 2 份循环（12 张），一份应占 50%；且 flex gap 方案下百分比位移总有半个 gap 误差，导致每 46s 动画跳回原点时画面回退一大截（用户感知为"刷新重置"）
- **修复**：`.project-marquee-track` 去掉 `gap`，卡片改 `margin-right: var(--pm-gap)`（含最后一张）；动画 `pm-marquee` 改为 `translateX(-50%) → 0`（向右滚动），-50% 精确等于 1 份循环宽度 → 无缝无跳变；删除 pm-row-reverse / pm-marquee-reverse 冗余代码
- 该方案对任意作品数量通用（loop 恒为 2 份，-50% 与 n 无关）
- 修改：`src/components/Projects.jsx`（去掉 pm-row-reverse 类）、`src/styles/components.css`；preview.html 已重建

## 关机恢复 + 两排反向滚动（已被本次推翻：改为同向向右）
- 关机未造成文件丢失/损坏：6 个 project-N 封面齐全，源文件/配置/样式完整
- localhost:5173 无法访问 = dev server 随关机停止，重启即可
- 两排滚动方向改为不同：第一排 `pm-row-reverse` 类 → `pm-marquee-reverse` 动画（translateX -33.3333% → 0，从左往右），第二排保持原方向（从右往左）
- 修改文件：`src/components/Projects.jsx`（第一排 div 加 `pm-row-reverse` 类）、`src/styles/components.css`（新增反向动画 + 移动端时长同步）
- 重新构建 preview.html

## Unity 作品集：React + Vite 重构
用户（林赫洋，Unity 客户端开发者）原本的纯静态作品集重构为 React + Vite 工程：
- 数据原样保留：`src/data.js`（来自原 `scripts/data.js`，内容一字不改）
- 之前未展示的 intro / skills / aiWorkflow / roadmap 数据现在都已渲染呈现，便于用户后续统一删减
- 暗色科技感设计系统：`src/styles/{tokens,base,components}.css`，teal + 冰蓝强调，深蓝黑背景，mono 字体编号标签，缓慢网格动效
- 版心 1700px，移动端适配（断点 1280/1080/900/640/420）
- 滚动 reveal 动效（Reveal 组件 + IntersectionObserver）+ Hero 加载动画 + 卡片 hover 发光 + 鼠标跟随光晕
- 工程结构：`vite.config.js`（react + vite-plugin-singlefile）、`index.html`（Vite 入口）、`src/main.jsx` 入口、`src/App.jsx` 组装、各板块组件在 `src/components/`、`public/assets/` 静态资源
- GitHub Pages workflow 改为构建部署：`pages.yml` 用 actions/setup-node + npm ci + npm run build + upload-pages-artifact + deploy-pages

## Bug 修复：双击根 index.html 白屏
- 原因：根 index.html 是 Vite 入口，引用未编译的 `/src/main.jsx`，浏览器以 file:// 协议打开时无法运行 ES module
- 解决：默认构建改为单文件（vite-plugin-singlefile），`dist/index.html` 是自包含的，双击即可打开；图片仍走相对路径
- 验证：puppeteer 用 file:// 协议加载 dist/index.html，#root 有内容、HERO_TITLE=林赫洋、9 个 section、无 console error
- README 明确说明：根 index.html 不能双击，预览用 dist/index.html

## 调试中的小坑
- vite build 时若 preview server 占用 dist，rmSync 被沙箱拦截；构建前需停掉 preview
- puppeteer fullPage 截图时 IntersectionObserver 与滚动时序错位，后续板块 opacity 残留；截图脚本强制给所有 .reveal 加 is-inview 后再截

## 补充：根目录可直接双击打开的入口
用户明确要求"点击 index 文件就能打开"，所以：
- 新增根目录 `preview.html`：由构建产物生成（JS/CSS 内联），资源引用改为 `./public/assets/`（指向源码资源），file:// 协议下双击即开
- 新增 `scripts/make-preview.mjs` + `npm run preview:file` 脚本（vite build && node scripts/make-preview.mjs）
- 验证：file:// 打开 preview.html → #root 有内容、9 个 section、6/6 图片加载、无 console error、无 failed request
- 再次踩坑：vite build 清理 dist 时若被进程占用（present_files 静态服务等）会 trash 失败；先 taskkill 占用端口的 PID + rm -rf dist 再 build

## 线上部署（分享给朋友）
- 用 workbuddy_cloudstudio_deploy 部署 dist/ → 分享链接 https://cef8d23b20d745739b6f8b4a0047ab8d.bj7.agentos-app.net（verified: true）
- 线上验证：title/hero/sections=9/imgs=6/6，无 console error
- 用户可在「设置 - 数据管理 - 我发布的应用」管理该应用
- 备注：用户希望朋友也能打开网站；后续如需长期稳定域名可考虑 GitHub Pages（workflow 已配好，需用户自己 push 到 GitHub）

## GitHub Pages 正式上线（最终方案）
- 最终 URL：https://fleecebell.github.io/portfolio/（Actions 自动构建部署，run #7 success）
- 关键坑记录：
  1. 本机 git CLI 无 GitHub 凭据（GCM 无缓存、无 gh、无 token）→ 无法直接 push
  2. GitHub 连接器（MCP）对该仓库写权限 403（"Resource not accessible by integration"），授权后仍 403，重连也没解决 → 放弃连接器推送
  3. 用户自己 push 时 commit "1" 误删了 index.html → vite build 报 "Could not resolve entry module index.html"
  4. 修复：git show e86b11a:index.html > index.html + commit d624c69
  5. Pages 源必须设为 GitHub Actions（之前是 gh-pages 分支模式，显示旧版）
  6. 最终用用户提供的 PAT（ghp_...）一次性 URL 推送成功：git push https://<token>@github.com/Fleecebell/portfolio.git main
- 用户本地 git 有 8 个 commit 领先历史（含重构 e86b11a + 修复 d624c69），origin/main 引用可能过期需 git fetch
- 提醒用户撤销 PAT（Settings → Developer settings → Personal access tokens → Delete）
## 后续内容微调（用户在我的基础上改 + 我继续调整）
- 用户的修改：Header 导航加 "00 自我介绍 #top"、data.js 简介微调、components.css 把 .timeline 改成 3 列网格卡片、.section 加 scroll-margin-top calc(100px - section-gap)、新增简历 PDF；全部保留
- 我的调整：删 Intro/Skills/AiWorkflow/Roadmap 4 模块、Hero 精简（删 hero-path/tagline 后半段/scroll-hint）、"联系与资料"按钮统一普通样式、删状态标签、"精选项目"→"项目与能力"、Hero 名片（"Unity Client Developer"→"Unity 客户端开发 · Unity Client Developer"、羊毛Fleece_→林赫洋、// Unity Client Developer→// Unity 客户端开发）
- 锚点定位：去掉 scroll-padding-top + scroll-margin-top 叠加，改为 scroll-padding-top:0 + 去掉 scroll-margin-top，标题统一 ~21% 位置；Contact 板块补 section-head 结构
- 数据保留（intro/skills/aiWorkflow/roadmap 字段未删，方便恢复）

## 视频背景（用户上传后转码）
- 用户原始视频 public/assets/hero-bg.mov：HEVC/H.265、3840×2160、5.64 秒、6.2 Mbps、4.4 MB
- Chrome 桌面无法解码 HEVC（也不接受 .mov 容器）→ 看不到效果
- 用 ffmpeg-static 转码：H.264 libx264 / 1920×1080 / crf 27 / -an（去音轨）/ +faststart → hero-bg.mp4 仅 206 KB
- 本地预览限制：file:// 协议下 Chrome 拒绝加载任何本地 <video>（"Media load rejected by URL safety check"）—— 双击 preview.html 看不到视频背景，但其他内容正常
- 本地看视频效果必须用 HTTP：npm run dev 或 npx vite preview
- 推送后线上（GitHub Pages）一切正常

## 视频背景层 2：fixed 全屏 + 玻璃面板
- 用户要求视频不随滚动走（fixed 全屏）且更亮、面板玻璃化
- 视频：position: fixed + z-index -1 + opacity 0.8 + saturate 0.85；遮罩改为 0.52/0.28/0.66
- 面板半透明：hero-card 0.58/0.66 + blur 8px、timeline-item 0.5/0.58 + blur 6px、project-card 0.4/0.55 + blur 10px、contact-panel 0.48/0.56 + blur 10px、project-media 0.42、project-tags 0.5、section-alt 0.38、site-header.scrolled 0.6
- 视觉效果：滚动到任何位置都能看到视频流动，面板像玻璃浮在上面

## 推送上线（17:13，commit 872b46c）
- 用户确认后提交推送（PAT 仍有效）：main → 872b46c，Actions run #9 success
- 线上验证 https://fleecebell.github.io/portfolio/：视频播放中(opacity 0.8, fixed)、导航 5 项、5 sections、简历 PDF 在、无蒙版无状态标签、6/6 图片、零错误零失败请求
- 移除 Experience/Projects 的 section-alt 深色蒙版（用户指出不需要）

## 移动端视频兜底 + 交付转码工具（17:3x，commit a470e4b 已推）
- 手机看不到视频原因：iOS/华为/微信 自动播放策略拦截（系统级，无法绕过）
- 兜底方案：ffmpeg 提取首帧 hero-bg-poster.jpg；video 加 poster；Hero useEffect 检测 3 秒未播放/未解码则淡入首帧静态图；首次 touch/click 后重试 play()
- 实测发现：file:// 下 mp4 可以播放（之前 .mov 被拒是容器问题）
- 用户决定自己推送，我不再代推
- 交付 scripts/convert-video.mjs（ffmpeg-static 已加入项目 devDependencies）：任意视频 → H.264 1080p mp4（去音轨 + faststart），用于背景视频和作品视频上传
- README 增加"怎么上传视频"章节，视频规范：H.264 mp4 / ≤1080p / <10MB / 无音轨

## 华为手机视频不动（17:43，本地已改未推）
- 华为手机（用户实测）：视频显示首帧静态图（兜底生效）→ 华为/微信浏览器拦截 muted 自动播放
- 增强交互重试（Hero.jsx 已改，未推送）：自动重试最长 8 秒（loadeddata/canplay/interval 1500ms）+ 任何交互（touchstart/click/scroll/keydown）立即重试播放
- 提醒用户：滑动或点击屏幕视频即开始流动；微信内置浏览器限制最严需点击

## 微信打开视频不动（17:47）
- 用户确认：微信打开视频不动、系统浏览器打开会动 → 微信内置浏览器禁止网页视频自动播放（产品策略，所有网站皆然），且要求 play() 必须在用户手势（非 passive touch/click）内调用
- 已改（未推）：touchstart/click 监听改为 capture + passive:false（让微信识别为用户手势）；scroll/keydown 保持 passive
- 结论：微信内无法自动播放背景视频，只能点击后播放；给朋友分享建议提示"用浏览器打开"
## 微信打开视频不动（17:47）
- 用户确认：微信打开视频不动、系统浏览器打开会动 → 微信内置浏览器禁止网页视频自动播放（产品策略，所有网站皆然），且要求 play() 必须在用户手势（非 passive touch/click）内调用
- 已改：touchstart/click 监听改为 capture + passive:false（让微信识别为用户手势）；scroll/keydown 保持 passive
- 后续增强：动画 WebP 兜底（340KB，640x360 libwebp_anim quality 72）；微信 UA 检测 100ms 内直接切 WebP（其他浏览器 800ms）；微信「点击播放高清背景」按钮放在导航栏汉堡按钮左侧

## 项目封面自动扫描系统（vite 虚拟模块 + marquee 轮播）
- vite.config.js projectCoversPlugin 扫描 public/assets/projects/project-N/cover.{jpg,jpeg,png,webp}
- 用户协议：文件夹名 project-数字、封面文件名 cover（固定）
- 新增 project-7/cover.jpg → 重新构建 → 自动并入轮播末尾，无需改代码
- Projects.jsx 重写：横向无缝 marquee（3 份内容循环、46s 动画）+ 悬停暂停 + 左下角显示类型+名称
- 验证：18 卡 3 份循环、悬停暂停生效、info 渲染正确（puppeteer 单步 mouse.move 不触发 mouseenter，需多步移动；真实用户连续移动无问题）
- 已生成 6 张占位封面到 project-1..6（Canvas 800x500），用户用真实封面替换即可
- README 新增"项目封面规范"章节
