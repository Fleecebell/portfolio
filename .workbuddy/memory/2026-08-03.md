
## 6 张项目封面生效（dev 重启 + preview 重建）
- 用户放入 public/assets/projects/project-1~6/cover.*（3/4/5 今天新增）
- 原因：封面由 virtual:project-covers 构建时扫描 → preview.html 需重新 build；dev server 已停止（8月1日后），重启 npm run dev 恢复 5173
- 已重建 preview.html 并验证 dist 含 6 张封面

## 6 张项目封面生效（dev 重启 + preview 重建）
- 用户放入 public/assets/projects/project-1~6/cover.*（3/4/5 今天新增）
- 原因：封面由 virtual:project-covers 构建时扫描 → preview.html 需重新 build；dev server 已停止（8月1日后），重启 npm run dev 恢复 5173
- 已重建 preview.html 并验证 dist 含 6 张封面

## 手动构建说明 + .gitkeep 清理
- 构建命令：npm run build:preview（构建+同步 preview.html+复制 assets）；npm run build（仅 dist）；npm run dev（实时预览）
- .gitkeep：git 空目录占位文件；project-1~6/resume 已有实际文件，占位已无用
- 已删除 public 下 5 个 gitkeep（project-1~4 + resume）；dist/assets 副本由 gitignore 忽略

## project-5 换图未生效修复
- 根因：用户把新图放错目录——放进了根 assets/projects/project-5/（构建副本，会被 sync 覆盖），正确源目录是 public/assets/projects/project-5/
- 处理：新图（cover.png）复制到 public 源目录；删除旧 cover.jpg（虚拟模块按 jpg/jpeg/png/webp 顺序优先 jpg，必须删旧图）；重建 preview.html 生效
- 提醒：换项目封面正确流程 = 覆盖 public/assets/projects/project-N/cover.*（只留一张）→ npm run build:preview

## 垃圾文件清单（用户备忘）
可随手删：dist-old-*（构建残留）、avatar-*.jpg（favicon 副本，sync 重建）、vite.config.js.timestamp-*.mjs（vite 崩溃残留）、shot-*.png（调试截图）、.gitkeep（目录有文件后）
注意：根 assets/ 别删（file:// preview.html 依赖，删了会 404 直到下次构建）；.workbuddy/ src/ public/ index.html 等绝不能删
已清：dist-old ×12 + avatar 副本

## 双端构建验证通过
- 重建 preview.html（6 张封面 project-1~6，project-5 为 cover.png）
- headless 双端验证（dev 5173 + file:// preview.html）：24 张轮播卡（6 项目×2行×2份）、封面图路径正确、broken 0、立牌 3D 渲染、视频播放、详情可开、叉叉可见 —— 两站完全一致

## file:// 旧图排查：构建正确，属浏览器缓存
- 验证：preview.html 引用 cover.png（新图）✓、public/dist/根assets 均为新图 ✓ → 构建正确
- 残留：根 assets 有历史 cover.jpg（sync 只复制不删除）→ 已删（未被引用，无害）
- 结论：用户 file:// 看到旧图 = 浏览器缓存了旧 preview.html/图片 → 需强制刷新（Ctrl+F5）

## project-5 封面损坏修复（PNG 编码兼容性）
- 现象：dev 显示损坏（naturalWidth 0）、file 旧图；dev 根因 = dev server 在换图前启动、虚拟模块缓存旧列表（cover.jpg 已删 → 404）
- 图片根因：用户 PNG 结构完整且 PIL 可解码（1515x845），但 Chromium 无法解码（IDAT 编码兼容问题）→ PIL 重编码为标准 PNG 后双端 naturalWidth 1515x845 ✓
- 处理：重启 dev server（重新扫描 public）；PIL 重编码 cover.png；重建 preview.html
- 原图备份：%TEMP%\cover-orig-project5.png
- 经验：venv 在 ~/.workbuddy/binaries/python/envs/default（已装 pillow 12.3.0）

## project-5 换图（jpg）+ 构建
- 用户又放错位置（根 assets 副本）→ 移到 public 源目录；删旧 cover.png（虚拟模块 jpg 优先，只留一张）
- 新图 cover.jpg 563KB（1516x846，PIL 解码正常）；构建成功（npx 输出被吞但 dist 生成）、preview 引用 cover.jpg、根 assets 干净
- 重启 dev server（清除虚拟模块旧列表，cover.jpg 200）

## 项目与能力整栏电脑端下移 100px
- 需求：电脑浏览器端 #projects（项目与能力）整栏下移 100px，其他不变，改动最小化
- 实现：在已有 @media (min-width: 641px) 块内加 #projects { margin-top: 100px }（与 #awards -180px 同块）；只新增 100px 一个数字，手机端（≤640px）不受影响
- 验证（CDP）：桌面 1440px margin-top 100px ✓；手机 390px 0px ✓
- 注意：构建遇 safe-delete 拦截（dist/assets trash 失败）→ 先 mv dist 再 build:preview（沿用旧方案）
- 修改：src/styles/components.css；preview.html 已重建

## 取消封面悬停暂停滚动
- 需求：鼠标放在作品封面时轮播不再停止滚动
- 实现：删除 components.css `.pm-rows.paused .project-marquee-track { animation-play-state: paused }`；Projects.jsx 移除 paused/setPaused 状态、onMouseEnter/Leave/TouchStart/End 里的 setPaused 调用、pm-rows 的 paused class（消除死代码 + 无效 re-render）
- 修改：src/styles/components.css、src/components/Projects.jsx；preview.html 已重建
- 附带：清理 dist-old-* 备份目录

## 删除 footer 中间的技术标注
- 需求：删除 footer 里 `$ unity-client portfolio · react + vite` 标注
- 实现：Footer.jsx 移除 `.f-mono` 的 span 元素（space-between 布局自动变为 2 端对齐：© 林赫洋 左 / Unity Client Portfolio 右）；.f-mono/.f-accent CSS 规则保留未删（无害，最小改动）
- 修改：src/components/Footer.jsx；preview.html 已重建
