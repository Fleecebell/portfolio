# 2026-08-07 工作日志

## 作品子页面填充：详情面板按 5 块结构渲染
- 需求：用户开始填充作品子页面，复用 data.js 现有数据（确认选择「复用现有数据」）
- 实现：Projects.jsx 详情面板从占位文案改为 5 块结构渲染：
  1. 游戏内容 → selected.data.summary（detail-summary）
  2. 工作内容 → selected.data.points（detail-points / dp-label / dp-text）
  3. 技术点 → selected.data.tags（detail-tags / tag）
  4. jam站点/获奖 → selected.data.links（detail-links / btn）
  5. 游戏图片 → selected.gallery（detail-media 竖排，已自动扫描）
- 每块包在 <section className="detail-block">：区块标题用 dp-label（mono 小标签 + 底部渐隐分隔线），块间 border-bottom 分隔
- data.js 数据结构（projects[i]）：name / type / role / status / links[] / tags[] / media{images,video} / summary / points[{label,text}]
- 修改：src/components/Projects.jsx、styles/components.css（新增 .detail-block 样式）；preview.html 已重建
- 后续填充流程：用户改 src/data.js 的文字 → dev HMR 自动生效 → 图片放 public/assets/projects/project-N/shot-*.png/jpg（虚拟模块需重启 dev 才扫到新图）

## 详情面板布局调整（按无限黄昏样板）
- 需求：①「工作内容」去掉卡片框，改纯段落（与游戏内容显示一致）②删除「技术点」区块 ③游戏图片移到详情顶部
- 实现：Projects.jsx 重排区块顺序：游戏图片（置顶）→ 游戏内容（summary）→ 工作内容（points 逐个渲染为 detail-summary 纯段落，不再用 detail-points 卡片）→ jam站点/获奖（links）；tags 区块整段删除
- 注意：无限黄昏的 points 已改为 { text } 无 label（用户精简了数据结构）；其他项目 points 仍带 label，渲染时只取 pt.text 所以无影响
- 修改：src/components/Projects.jsx；preview.html 已重建

## 详情面板区块顺序再调整
- 需求：游戏图片移到「工作内容」和「jam站点/获奖」之间（不是顶部）
- 最终顺序：游戏内容 → 工作内容 → 游戏图片 → jam站点/获奖
- 修改：src/components/Projects.jsx（图片区块从顶部移到 links 区块前）；preview.html 已重建

## 游戏图片改两列网格
- 需求：图片小一点、一排两张
- 实现：.detail-media 从 flex column（竖排大图）改 grid 2 列（repeat(2, 1fr)，gap 14px，每张等比缩放占半宽）
- 修改：src/styles/components.css；preview.html 已重建

## 图片点击放大（lightbox）
- 需求：点击游戏图片放大打开，而不是退出详情面板
- 实现：Projects.jsx 加 zoomImg state（Escape 优先关放大、再关详情）；图片 onClick 设 zoomImg + stopPropagation（否则冒泡到面板点透关闭）；portal 渲染 .detail-zoom 全屏遮罩（z500，高于详情 z200/导航 z100），点击遮罩或图关闭；CSS 加 zoom-in/zoom-out 光标 + hover 微缩放
- 修改：src/components/Projects.jsx、styles/components.css；preview.html 已重建
